# Deployment trigger

This marker exists to trigger the production Cloudflare deployment workflow after deployment configuration was restored.

The deployment target is the Cloudflare Workers `workers.dev` environment; the custom domain remains out of the critical launch path.
