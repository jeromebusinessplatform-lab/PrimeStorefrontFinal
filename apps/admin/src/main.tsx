import React, { FormEvent, useEffect, useState } from "react";
import { createRoot } from "react-dom/client";
import { CatalogPanel } from "./catalog-panel";
import { LoyaltyPanel } from "./loyalty-panel";
import { OrderManagementPanel } from "./order-management-panel";
import "./styles.css";

type Warehouse = { id: string; name: string; address: string; latitude: number; longitude: number; isDefault: boolean; isActive: boolean };
type Courier = { id: string; name: string; type: "standard" | "express" | "priority"; baseFeeMinor: number; perKmRateMinor: number; platformFeeMinor: number; surchargeMinor: number; isActive: boolean };
type ApiError = { error?: string };
function inputElement(id: string): HTMLInputElement { return document.getElementById(id) as HTMLInputElement; }
function selectElement(id: string): HTMLSelectElement { return document.getElementById(id) as unknown as HTMLSelectElement; }
async function responseError(response: Response, fallback: string): Promise<Error> {
  const body = await response.json().catch(() => null) as ApiError | null;
  return new Error(body?.error ?? fallback);
}
async function login(accessCode: string): Promise<void> {
  const response = await fetch("/admin/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ accessCode }), credentials: "include" });
  if (!response.ok) throw await responseError(response, "admin_auth_failed");
}
async function hasSession(): Promise<boolean> {
  const response = await fetch("/admin/auth/session", { credentials: "include", cache: "no-store" });
  return response.ok;
}
async function getDeliveryConfig(): Promise<{ warehouses: Warehouse[]; couriers: Courier[] }> {
  const [warehousesResponse, couriersResponse] = await Promise.all([
    fetch("/admin/delivery/warehouses", { credentials: "include", cache: "no-store" }),
    fetch("/admin/delivery/couriers", { credentials: "include", cache: "no-store" }),
  ]);
  if (!warehousesResponse.ok) throw await responseError(warehousesResponse, "warehouse_load_failed");
  if (!couriersResponse.ok) throw await responseError(couriersResponse, "courier_load_failed");
  const warehousesBody = await warehousesResponse.json() as { warehouses: Warehouse[] };
  const couriersBody = await couriersResponse.json() as { couriers: Courier[] };
  return { warehouses: warehousesBody.warehouses, couriers: couriersBody.couriers };
}
async function createWarehouse(input: Record<string, unknown>): Promise<void> { const response = await fetch("/admin/delivery/warehouses", { method: "POST", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify(input) }); if (!response.ok) throw await responseError(response, "warehouse_create_failed"); }
async function createCourier(input: Record<string, unknown>): Promise<void> { const response = await fetch("/admin/delivery/couriers", { method: "POST", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify(input) }); if (!response.ok) throw await responseError(response, "courier_create_failed"); }
async function setDefaultWarehouse(id: string): Promise<void> { const response = await fetch(`/admin/delivery/warehouses/${encodeURIComponent(id)}/default`, { method: "POST", credentials: "include" }); if (!response.ok) throw await responseError(response, "warehouse_default_failed"); }
async function updateWarehouse(id: string, input: Record<string, unknown>): Promise<void> { const response = await fetch(`/admin/delivery/warehouses/${encodeURIComponent(id)}`, { method: "PATCH", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify(input) }); if (!response.ok) throw await responseError(response, "warehouse_update_failed"); }
async function updateCourier(id: string, input: Record<string, unknown>): Promise<void> { const response = await fetch(`/admin/delivery/couriers/${encodeURIComponent(id)}`, { method: "PATCH", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify(input) }); if (!response.ok) throw await responseError(response, "courier_update_failed"); }

function DeliveryManagement() {
  const [warehouses, setWarehouses] = useState<Warehouse[]>([]);
  const [couriers, setCouriers] = useState<Courier[]>([]);
  const [loading, setLoading] = useState(true);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [editingWarehouse, setEditingWarehouse] = useState<string | null>(null);
  const [editingCourier, setEditingCourier] = useState<string | null>(null);
  async function reload() { setError(null); try { const data = await getDeliveryConfig(); setWarehouses(data.warehouses); setCouriers(data.couriers); } catch (e) { setError(e instanceof Error ? e.message : "delivery_config_load_failed"); } finally { setLoading(false); } }
  useEffect(() => { void reload(); }, []);
  async function action(work: () => Promise<void>) { setBusy(true); setError(null); try { await work(); await reload(); } catch (e) { setError(e instanceof Error ? e.message : "delivery_operation_failed"); } finally { setBusy(false); } }
  return <section className="panel delivery-panel" aria-labelledby="delivery-title"><div className="section-heading"><div><div className="eyebrow">DELIVERY</div><h2 id="delivery-title">Warehouse & Courier</h2></div><button type="button" onClick={() => void reload()} disabled={loading || busy}>Refresh</button></div>{error ? <p className="error" role="alert">{error}</p> : null}{loading ? <p className="muted">Loading delivery configuration…</p> : <><div className="delivery-columns"><div><h3>Warehouses</h3><p className="muted">Exactly one active default is used as the routing origin.</p>{warehouses.map((warehouse) => editingWarehouse === warehouse.id ? <div className="delivery-item" key={warehouse.id}><input aria-label="Warehouse name" defaultValue={warehouse.name} id={`wh-name-${warehouse.id}`} /><input aria-label="Warehouse address" defaultValue={warehouse.address} id={`wh-address-${warehouse.id}`} /><div className="inline-fields"><input aria-label="Latitude" defaultValue={warehouse.latitude} id={`wh-lat-${warehouse.id}`} inputMode="decimal" /><input aria-label="Longitude" defaultValue={warehouse.longitude} id={`wh-lon-${warehouse.id}`} inputMode="decimal" /></div><div className="button-row"><button type="button" onClick={() => void action(() => updateWarehouse(warehouse.id, { name: inputElement(`wh-name-${warehouse.id}`).value, address: inputElement(`wh-address-${warehouse.id}`).value, latitude: Number(inputElement(`wh-lat-${warehouse.id}`).value), longitude: Number(inputElement(`wh-lon-${warehouse.id}`).value) }))} disabled={busy}>Save</button><button type="button" onClick={() => setEditingWarehouse(null)} disabled={busy}>Cancel</button></div></div> : <div className="delivery-item" key={warehouse.id}><strong>{warehouse.name}</strong><span className="muted">{warehouse.address}</span><span className="muted">{warehouse.latitude.toFixed(5)}, {warehouse.longitude.toFixed(5)} {warehouse.isDefault ? "• DEFAULT" : ""}</span><div className="button-row"><button type="button" onClick={() => setEditingWarehouse(warehouse.id)} disabled={busy}>Edit</button>{!warehouse.isDefault && warehouse.isActive ? <button type="button" onClick={() => void action(() => setDefaultWarehouse(warehouse.id))} disabled={busy}>Set Default</button> : null}</div></div>)}<div className="delivery-item create-item"><strong>Add warehouse</strong><input id="new-wh-name" aria-label="New warehouse name" placeholder="Name" /><input id="new-wh-address" aria-label="New warehouse address" placeholder="Address" /><div className="inline-fields"><input id="new-wh-lat" aria-label="New latitude" placeholder="Latitude" inputMode="decimal" /><input id="new-wh-lon" aria-label="New longitude" placeholder="Longitude" inputMode="decimal" /></div><button type="button" onClick={() => void action(() => createWarehouse({ name: inputElement("new-wh-name").value, address: inputElement("new-wh-address").value, latitude: Number(inputElement("new-wh-lat").value), longitude: Number(inputElement("new-wh-lon").value) }))} disabled={busy}>Add Warehouse</button></div></div><div><h3>Couriers</h3><p className="muted">Pricing is stored in PHP minor units (₱100 = 10000).</p>{couriers.map((courier) => editingCourier === courier.id ? <div className="delivery-item" key={courier.id}><input aria-label="Courier name" defaultValue={courier.name} id={`co-name-${courier.id}`} /><select aria-label="Courier type" defaultValue={courier.type} id={`co-type-${courier.id}`}><option value="standard">Standard</option><option value="express">Express</option><option value="priority">Priority</option></select><div className="inline-fields"><input aria-label="Base fee minor" defaultValue={courier.baseFeeMinor} id={`co-base-${courier.id}`} inputMode="numeric" /><input aria-label="Per kilometer rate minor" defaultValue={courier.perKmRateMinor} id={`co-km-${courier.id}`} inputMode="numeric" /></div><div className="inline-fields"><input aria-label="Platform fee minor" defaultValue={courier.platformFeeMinor} id={`co-platform-${courier.id}`} inputMode="numeric" /><input aria-label="Surcharge minor" defaultValue={courier.surchargeMinor} id={`co-surcharge-${courier.id}`} inputMode="numeric" /></div><div className="button-row"><button type="button" onClick={() => void action(() => updateCourier(courier.id, { name: inputElement(`co-name-${courier.id}`).value, type: selectElement(`co-type-${courier.id}`).value, baseFeeMinor: Number(inputElement(`co-base-${courier.id}`).value), perKmRateMinor: Number(inputElement(`co-km-${courier.id}`).value), platformFeeMinor: Number(inputElement(`co-platform-${courier.id}`).value), surchargeMinor: Number(inputElement(`co-surcharge-${courier.id}`).value) }))} disabled={busy}>Save</button><button type="button" onClick={() => setEditingCourier(null)} disabled={busy}>Cancel</button></div></div> : <div className="delivery-item" key={courier.id}><strong>{courier.name}</strong><span className="muted">{courier.type.toUpperCase()}</span><span className="muted">Base ₱{(courier.baseFeeMinor / 100).toFixed(2)} • ₱{(courier.perKmRateMinor / 100).toFixed(2)}/km</span><span className="muted">Platform ₱{(courier.platformFeeMinor / 100).toFixed(2)} • Surcharge ₱{(courier.surchargeMinor / 100).toFixed(2)}</span><div className="button-row"><button type="button" onClick={() => setEditingCourier(courier.id)} disabled={busy}>Edit</button></div></div>)}<div className="delivery-item create-item"><strong>Add courier</strong><input id="new-co-name" aria-label="New courier name" placeholder="Name" /><select id="new-co-type" aria-label="New courier type" defaultValue="standard"><option value="standard">Standard</option><option value="express">Express</option><option value="priority">Priority</option></select><div className="inline-fields"><input id="new-co-base" aria-label="Base fee" placeholder="Base fee minor" inputMode="numeric" /><input id="new-co-km" aria-label="Per kilometer rate" placeholder="Per km minor" inputMode="numeric" /></div><div className="inline-fields"><input id="new-co-platform" aria-label="Platform fee" placeholder="Platform fee minor" inputMode="numeric" defaultValue="0" /><input id="new-co-surcharge" aria-label="Surcharge" placeholder="Surcharge minor" inputMode="numeric" defaultValue="0" /></div><button type="button" onClick={() => void action(() => createCourier({ name: inputElement("new-co-name").value, type: selectElement("new-co-type").value, baseFeeMinor: Number(inputElement("new-co-base").value), perKmRateMinor: Number(inputElement("new-co-km").value), platformFeeMinor: Number(inputElement("new-co-platform").value), surchargeMinor: Number(inputElement("new-co-surcharge").value) }))} disabled={busy}>Add Courier</button></div></div></div></>}</section>;
}

function App() {
  const [authenticated, setAuthenticated] = useState(false);
  const [accessCode, setAccessCode] = useState("");
  const [busy, setBusy] = useState(true);
  const [error, setError] = useState<string | null>(null);
  useEffect(() => { void hasSession().then(setAuthenticated).catch(() => setAuthenticated(false)).finally(() => setBusy(false)); }, []);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(null); try { await login(accessCode); setAccessCode(""); setAuthenticated(true); } catch (e) { setError(e instanceof Error ? e.message : "Access denied. Check the Admin Access Code."); } finally { setBusy(false); } }
  if (busy && !authenticated) return <main className="shell"><section className="panel">Checking session…</section></main>;
  if (!authenticated) return <main className="shell auth-shell"><section className="panel auth-panel" aria-labelledby="admin-title"><div className="eyebrow">PRIME ADMIN</div><h1 id="admin-title">Admin Access</h1><p className="muted">Enter the Admin Access Code to continue.</p><form className="stack" onSubmit={submit}><label className="field"><span>Admin Access Code</span><input autoComplete="one-time-code" inputMode="text" type="password" value={accessCode} onChange={(event) => setAccessCode(event.target.value)} required /></label><button className="primary" type="submit" disabled={busy}>{busy ? "Verifying…" : "Enter Admin"}</button></form>{error ? <p className="error" role="alert">{error}</p> : null}</section></main>;
  return <main className="shell"><header className="topbar"><div><div className="eyebrow">PRIME ADMIN</div><h1>Control Center</h1></div><span className="status">Authenticated</span></header><OrderManagementPanel /><section className="stack cards" aria-label="Admin modules"><article className="panel compact-card"><strong>Catalog</strong><span className="muted">Products, categories, and media.</span></article><article className="panel compact-card"><strong>Inventory</strong><span className="muted">Stock levels and movement history.</span></article><article className="panel compact-card"><strong>Payments</strong><span className="muted">Payment proofs and review decisions.</span></article><article className="panel compact-card"><strong>POS</strong><span className="muted">Walk-in order operations.</span></article></section><CatalogPanel /><LoyaltyPanel /><DeliveryManagement /></main>;
}

createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>);
